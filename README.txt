Display on the screen the message "Hello World!". Run the application. If it works, go to step 2 
- System.out.println("Hello World");


Define an array of strings languages, containing {"C", "C++", "C#", "Python", "Go", "Rust", "JavaScript", "PHP", "Swift", "Java"}
- Am creat un vector de stringuri numit langauges ce contine limbajele de programare

Generate a random integer n: int n = (int) (Math.random() * 1_000_000);
- Am creat o variabila n ce genereaza un nr random

Compute the result obtained after performing the following calculations:
  multiply n by 3;
  add the binary number 10101 to the result;
  add the hexadecimal number FF to the result;
  multiply the result by 6;

 - Am folosit 0b10101 pentru numarul binar si 0xFF pentru numarul hexadecimal

Compute the sum of the digits in the result obtained in the previous step. This is the new result. While the new result has more than one digit, continue to sum the digits of the result.

- Am scris codul cifrei de control 

Display on the screen the message: "Willy-nilly, this semester I will learn " + languages[result].

 - System.out.println("Willy-nilly, this semester I will learn " + languages[n]);
